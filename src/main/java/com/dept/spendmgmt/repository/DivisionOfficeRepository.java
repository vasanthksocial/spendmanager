package com.dept.spendmgmt.repository;

import com.dept.spendmgmt.model.DivisionOffice;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DivisionOfficeRepository extends JpaRepository<DivisionOffice, Long> {
}
